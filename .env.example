# Generation. Free tier ~30 RPM / ~500K tokens per day — see docs/BUDGET.md
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
# Optional second provider so a rate limit never blocks an eval run
GEMINI_API_KEY=
